const menu=document.querySelector(".menu-toggle");const nav=document.querySelector("#nav");menu.addEventListener("click",()=>{const open=menu.getAttribute("aria-expanded")!=="true";menu.setAttribute("aria-expanded",String(open));menu.setAttribute("aria-label",open?"Закрыть меню":"Открыть меню");nav.classList.toggle("open",open)});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");menu.setAttribute("aria-expanded","false")}));document.querySelectorAll('.carousel-controls button').forEach(button=>button.addEventListener('click',()=>{const carousel=document.getElementById(button.dataset.target);const first=carousel.querySelector('article');if(!first)return;const gap=parseFloat(getComputedStyle(carousel).columnGap)||0;const direction=button.classList.contains('carousel-next')?1:-1;carousel.scrollBy({left:direction*(first.getBoundingClientRect().width+gap),behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'})}));
